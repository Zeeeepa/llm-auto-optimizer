# LLM-Auto-Optimizer Environment Configuration
#
# Copy to .env and fill in values for local development.
# In production, these are provided via Secret Manager and Cloud Run env vars.
#
# DO NOT commit .env to version control!

# ============================================================================
# Service Configuration
# ============================================================================

# Service identification
SERVICE_NAME=llm-auto-optimizer
SERVICE_VERSION=1.0.0

# Environment (dev | staging | prod)
PLATFORM_ENV=dev

# HTTP server port
PORT=8080

# Node environment
NODE_ENV=development

# ============================================================================
# RuVector Service Integration (REQUIRED)
# ============================================================================

# RuVector service base URL
# Production: Provided via Secret Manager
RUVECTOR_SERVICE_URL=https://ruvector.agentics-dev.run.app

# RuVector API key for authentication
# Production: Provided via Secret Manager
RUVECTOR_API_KEY=your-ruvector-api-key-here

# ============================================================================
# Telemetry & Observability
# ============================================================================

# LLM-Observatory endpoint for telemetry ingestion
# Production: Provided via Secret Manager
TELEMETRY_ENDPOINT=https://observatory.agentics-dev.run.app

# Enable JSON structured logging
JSON_LOGGING=true

# Log level (debug | info | warn | error)
LOG_LEVEL=info

# ============================================================================
# Platform Integrations (Read-Only Inputs)
# ============================================================================

# LLM-Observatory SDK (telemetry source)
# OBSERVATORY_API_KEY=optional-if-authenticated-via-service-account

# Latency-Lens SDK (latency metrics source)
# LATENCY_LENS_ENDPOINT=https://latency-lens.agentics-dev.run.app

# Cost-Ops SDK (cost metrics source)
# COST_OPS_ENDPOINT=https://cost-ops.agentics-dev.run.app

# ============================================================================
# Development Only
# ============================================================================

# Enable debug mode (additional logging)
DEBUG=false

# Skip ruvector-service health check on startup
# SKIP_RUVECTOR_CHECK=false
